const Loading = () => {
  return (
    <div className="flex flex-col items-center justify-center">
      <p className="heading-text text-yellow text-center">loading...</p>
    </div>
  )
}

export default Loading
